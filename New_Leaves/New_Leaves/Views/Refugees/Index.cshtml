@model IEnumerable<New_Leaves.Models.Refugee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RefugeeFName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RefugeeLName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RefugeeFName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RefugeeLName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RefugeeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RefugeeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RefugeeID })
        </td>
    </tr>
}

</table>
